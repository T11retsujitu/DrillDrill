#include "first_image.hpp"

#include <stdexcept>

// TODO(you): このセクションの課題です。README.md を読んで実装してください。
//   要件: 空でない 256x256 の 8bit(CV_8U) 1ch 画像を返す。中身は何でもよい。
//   ヒント: dd::synth::make_line_space(...) で作り、cv::normalize → convertTo(CV_8U)。
cv::Mat make_first_image() {
  throw std::logic_error(
      "TODO(you): make_first_image を実装してください（sections/00_setup/README.md 参照）");
}
