# solutions ブランチ

このブランチは、各セクションのドリル実装（`sections/**/src/`）に**参照解答**を入れた状態です。
`main`/開発ブランチはスタブ（`TODO(you)`）で、学習者が自分で実装します。

## 使い方（自力で答え合わせしたいとき）

```bash
# 特定セクションの解答だけを見る
git show solutions:sections/01_image_memory/src/brightness.cpp

# 開発ブランチとの差分（＝解答部分）を見る
git diff <開発ブランチ> solutions -- sections/
```

## 注意（自分への約束）

- **十分に自力で試したうえで**参照すること。ヒント → コード断片 → 解答 の順が理想。
- 解答を丸写しせず、なぜその実装になるかを説明できる状態を目指すこと。
- 迷ったら開発ブランチに戻り、Claude Code に段階的なヒントを求めるのが第一選択。

このブランチにあるのは、開発ブランチと同じテストを**参照解答で緑にできる**ことの保証でもあります。
